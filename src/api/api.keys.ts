export enum QueryKeyEnum {
  // Eslint issue with objects or enum props
  // eslint-disable-next-line no-unused-vars
  GET_PROJECTS = 'GET_PROJECTS',
  // eslint-disable-next-line no-unused-vars
  GET_GAMES = 'GET_GAMES',
}
